<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe Details</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            background-color: #f5f5f5;
            color: #333;
            padding: 20px;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #ff6b6b, #ff8e53);
            color: white;
            padding: 20px;
        }

        .user-info {
            background: rgba(255, 255, 255, 0.1);
            padding: 10px;
            border-radius: 8px;
            margin-top: 10px;
            font-size: 0.9em;
        }

        .recipe-content {
            padding: 30px;
        }

        .recipe-title {
            color: #2c3e50;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid #eee;
        }

        .recipe-meta {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .meta-item {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            border-left: 4px solid #ff6b6b;
        }

        .meta-item h3 {
            color: #ff6b6b;
            font-size: 0.9em;
            margin-bottom: 5px;
        }

        .recipe-detail {
            margin-bottom: 25px;
        }

        .recipe-detail h3 {
            color: #2c3e50;
            margin-bottom: 10px;
        }

        .recipe-image {
            width: 100%;
            max-height: 400px;
            object-fit: cover;
            border-radius: 12px;
            margin: 20px 0;
        }

        .action-buttons {
            display: flex;
            gap: 10px;
            margin-top: 30px;
        }

        .btn {
            padding: 10px 20px;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
        }

        .btn-edit {
            background: #4CAF50;
            color: white;
        }

        .btn-delete {
            background: #ff4757;
            color: white;
        }

        .btn-home {
            background: #2c3e50;
            color: white;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
        }

        .creator-info {
            margin-top: 20px;
            padding-top: 20px;
            border-top: 1px solid #eee;
            font-size: 0.9em;
            color: #666;
        }

        @media (max-width: 768px) {
            .container {
                margin: 10px;
                border-radius: 10px;
            }

            .recipe-meta {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Welcome, <%= user.name %></h1>
            <div class="user-info">
                Logged in through <b><%= user.type %></b> | User ID: <%= user.id %>
            </div>
        </div>

        <div class="recipe-content">
            <h2 class="recipe-title"><%= recipe.title %></h2>

            <div class="recipe-meta">
                <div class="meta-item">
                    <h3>RECIPE ID</h3>
                    <p><%= recipe.recipeid %></p>
                </div>
                <div class="meta-item">
                    <h3>CATEGORY</h3>
                    <p><%= recipe.category %></p>
                </div>
                <div class="meta-item">
                    <h3>CUISINE TYPE</h3>
                    <p><%= recipe.cuisine_type %></p>
                </div>
                <div class="meta-item">
                    <h3>PREP TIME</h3>
                    <p><%= recipe.preparation_time %></p>
                </div>
            </div>

            <div class="recipe-detail">
                <h3>Ingredients</h3>
                <p><%= recipe.ingredients %></p>
            </div>

            <div class="recipe-detail">
                <h3>Instructions</h3>
                <p><%= recipe.instructions %></p>
            </div>

            <% if (recipe.photo) { %>
                <img class="recipe-image" src="data:image/jpg;base64, <%= recipe.photo %>" alt="Recipe Image">
            <% } %>

            <% if (recipe.userid) { %>
                <div class="creator-info">
                    <p>Created by User ID: <%= recipe.userid %></p>
                </div>
            <% } %>

            <div class="action-buttons">
                <a href="/edit?_id=<%= recipe._id %>" class="btn btn-edit">Edit Recipe</a>
                <a href="/delete?_id=<%= recipe._id %>" class="btn btn-delete">Delete Recipe</a>
                <a href="/" class="btn btn-home">Back to Home</a>
            </div>
        </div>
    </div>
</body>
</html>